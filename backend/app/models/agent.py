"""Agent model (a configured LangGraph agent).

TODO: checklist "Agent runtime on LangGraph".
"""

# from app.models import Base


class Agent:  # TODO: subclass Base, __tablename__ = "agents"
    """Placeholder attributes: id, org_id, name, system_prompt, model,
    enabled_tools (JSON), memory_config (JSON), created_at.
    """

    # TODO: map columns; validate enabled_tools against the tool registry
    pass
