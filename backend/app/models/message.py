"""Message model (a turn within a Conversation).

TODO: checklist "Dashboard: Conversations".
"""

# from app.models import Base


class Message:  # TODO: subclass Base, __tablename__ = "messages"
    """Placeholder attributes: id, conversation_id, role (user/assistant/tool),
    content, tool_calls (JSON), created_at.
    """

    # TODO: map columns + index on conversation_id
    pass
