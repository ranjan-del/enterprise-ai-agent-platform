// Logs page.
// TODO: checklist "Execution history" — show agent execution traces.
import { Component } from '@angular/core';

@Component({
  selector: 'app-logs',
  standalone: true,
  template: `<h1>Logs</h1><!-- TODO: execution history table + trace viewer -->`,
})
export class LogsComponent {}
