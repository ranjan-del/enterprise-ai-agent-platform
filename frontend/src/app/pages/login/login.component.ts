// Login page.
// TODO: checklist "Auth + multi-tenancy" — wire form to AuthService.login().
import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  template: `<h1>Login</h1><!-- TODO: credential form + org selection -->`,
})
export class LoginComponent {}
