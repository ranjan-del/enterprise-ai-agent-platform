// Agents page.
// TODO: checklist "Agent runtime on LangGraph" — configure agents + tools.
import { Component } from '@angular/core';

@Component({
  selector: 'app-agents',
  standalone: true,
  template: `<h1>Agents</h1><!-- TODO: agent list + config editor -->`,
})
export class AgentsComponent {}
