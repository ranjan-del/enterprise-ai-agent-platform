// Chat page.
// TODO: checklist "streaming" — stream agent responses via ChatService.
import { Component } from '@angular/core';

@Component({
  selector: 'app-chat',
  standalone: true,
  template: `<h1>Chat</h1><!-- TODO: message list + composer + streaming -->`,
})
export class ChatComponent {}
