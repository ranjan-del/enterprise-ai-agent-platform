// Chat + streaming service.
// TODO: checklist "streaming" — consume SSE/WebSocket from the backend.
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ChatService {
  // TODO: sendMessage(conversationId, text) -> Observable<token stream>.
  // TODO: listConversations(), createConversation().
}
